## tmux version

The config files works with tmux > 2.4

## Installing using Ansible ##

Command:

ansible-playbook install-tmux.yaml --extra-vars "userdir=<USER_DIR>" --inventory <INVENTORY_FILE> --user <REMOTE_USER> --become

Example:

ansible-playbook install-tmux.yaml --extra-vars "userdir=/home/student" --inventory hosts --user student --become
